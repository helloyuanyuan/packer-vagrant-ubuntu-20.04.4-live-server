Vagrant.configure("2") do |config|
  config.vm.box = "ubuntu20044liveserver"
  config.vm.define "ubuntu20044masternode"
  config.vm.hostname = "ubuntu20044masternode"

  config.vm.network "public_network"

  config.vm.provider "virtualbox" do |vb|
    vb.name = "ubuntu20044masternode"
    vb.memory = 2048
    vb.cpus = 1
  end

end
